#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int p;

	for (p = 'o'; p <= '9'; p++)
		putchar(p);
	putchar('\n');
	return (0);
}
